<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.safespace">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission. ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".SafeSpaceApplication"
        android:allowBackup="true"
        android:icon="@mipmap/safespace_ic"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/safespace_ic_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SafeSpace">
        <activity android:name=".forgotPassScreen"></activity>
        <activity android:name=".registerScreen" />
        <activity android:name=".friendsScreen" />
        <activity android:name=".myInfoScreen" />
        <activity android:name=".homeScreen" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>